@model IEnumerable<CADENA_LIBRERIAS>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ID_LIBRERIA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NOMBRE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DESCRIPCION)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ID_LIBRERIA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NOMBRE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DESCRIPCION)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.ID_LIBRERIA  }) |
            @Html.ActionLink("Details", "Details", new {  id=item.ID_LIBRERIA  }) |
            @Html.ActionLink("Delete", "Delete", new {  id=item.ID_LIBRERIA  }) | 
            @Html.ActionLink("Ver catálogo", "Catalogue", new {  id=item.ID_LIBRERIA  }) | 
            <!--Html.ActionLink("Agregar Libro", "AddBook", new {  id=item.ID_LIBRERIA  }) | -->
        </td>
    </tr>
}

</table>
